export enum FrequencyLabelEnum {
  DAYS = "Dias",
  WEEKS = "Semanas",
  MONTHS = "Meses",
  YEARS = "Anos",
}

export enum FrequencyEnum {
  DAYS = "days",
  WEEKS = "weeks",
  MONTHS = "months",
  YEARS = "years",
}

export enum FrequencyPeriodEnum {
  DAYS = 366,
  WEEKS = 4,
  MONTHS = 12,
  YEARS = 40,
}
